> **Deprecated — FlowGraph v1 only.** v2 uses scripted `scenario` steps instead of runtime simulation. See [`SCENARIO.md`](SCENARIO.md).

# Simulation (v1)

This document described the v1 token simulation engine (`sources`, `admission`, `gate`, `emit`). It was removed in v2.0.0.

For the current model, read [`SCENARIO.md`](SCENARIO.md).
